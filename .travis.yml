language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install -e .
  - pip install nosetests
# command to run tests
script:
  - python3 -m nose --exe
